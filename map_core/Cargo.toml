[package]
name = "map_core"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
common.workspace = true
anyhow.workspace = true
serde.workspace = true
# Editable documents keep their authored key order through every operation.
serde_json = { workspace = true, features = ["preserve_order"] }
