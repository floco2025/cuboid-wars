[package]
name = "server"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
common = { path = "../common" }
bincode = "2.0"
renet = "2"
renet_netcode = "2"
crossbeam-channel = "0.5"
anyhow = "1"
bevy = { version = "0.19", default-features = false, features = ["bevy_core_pipeline"] }
rand = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
